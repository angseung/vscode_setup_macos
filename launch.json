{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "main",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/Debug/bin/main",
            "cwd": "${fileDirname}",
        }
    ]
}