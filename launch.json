{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "main",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/Debug/bin/main",
            "cwd": "${workspaceFolder}",
            "args": "[]"
        }
    ]
}